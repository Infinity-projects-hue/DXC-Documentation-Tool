import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export default function SettingsPage() {
  return (
    <PlaceholderPage
      eyebrow="Settings"
      title="Preferences & Profile"
      description="Theme, default export format, AI model preference, notification rules and connected accounts."
      hint="Theme toggle is live in the header today — full settings panel is coming next."
      status="Coming Soon"
    />
  );
}
